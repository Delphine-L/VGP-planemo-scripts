 # VGP Planemo Scripts - Python Dependencies

# Core dependencies
bioblend>=1.0.0
pandas>=1.0.0
pyyaml>=5.1
requests>=2.20.0

# Planemo for workflow execution
planemo>=0.74.0

# AWS CLI for GenomeArk data access (required for --fetch-urls option)
awscli>=1.27.0

# NOTE: NCBI datasets command-line tool is also required for workflow 9 (decontamination)
# This is a standalone binary, not a Python package
# Install using the provided installs.sh script or manually from:
# https://www.ncbi.nlm.nih.gov/datasets/docs/v2/download-and-install/
